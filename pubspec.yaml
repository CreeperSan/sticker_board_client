name: sticker_board_client
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.2

  # lib from base layer
  device_information:
    path: base/device_information
  kv_storage:
    path: base/kv_storage
  log:
    path: base/log
  ui_lib:
    path: base/ui_lib
  toast:
    path: base/toast
  lifecycle:
    path: base/lifecycle
  network:
    path: base/network
  i18n:
    path: base/i18n

  # lib from business layer
  account:
    path: business/account
  account_api:
    path: business/account_api
  splash_screen:
    path: business/splash_screen
  version:
    path: business/version
  version_api:
    path: business/version_api
  sticker_board:
    path: business/sticker_board
  sticker_board_api:
    path: business/sticker_board_api
  file_uploader:
    path: business/file_uploader
  url_builder:
    path: business/url_builder
  application_config:
    path: business/application_config

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/i18n/
