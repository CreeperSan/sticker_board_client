name: third_libs
description: A new Flutter package project.
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.6
  dio: ^4.0.0
  oktoast: ^3.1.1
  provider: ^5.0.0
  flutter_staggered_grid_view: ^0.4.0
  path_provider: ^2.0.3
  url_launcher: ^6.0.12

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

