name: sticker_board
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  sticker_board_api:
    path: ../sticker_board_api
  network:
    path: ../../base/network
  log:
    path: ../../base/log
  ui_lib:
    path: ../../base/ui_lib
  toast:
    path: ../../base/toast
  third_libs:
    path: ../../base/third_libs

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:


